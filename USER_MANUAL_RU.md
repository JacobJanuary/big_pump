# Руководство Пользователя: Система Анализа Big Pump

Это руководство описывает полный цикл работы с системой: от обновления данных до оптимизации стратегии и бэктестинга.

## 🚀 Быстрый Старт (Ежедневная Рутина)

Чтобы поддерживать данные в актуальном состоянии и видеть свежую статистику, выполняйте эти шаги раз в день или раз в несколько дней.

### 1. Обновление базы сигналов
Скрипт загружает новые сигналы из основной базы и рассчитывает их метрики (максимальный рост, просадка).
```bash
python3 scripts/populate_signal_analysis.py --days 30
```
*   `--days 30`: Загрузить сигналы за последние 30 дней.
*   Скрипт работает в **инкрементальном режиме**: он добавляет только новые сигналы, не дублируя старые.

### 2. Загрузка минутных свечей
Для точной симуляции нужны 1-минутные свечи с Binance.
```bash
python3 scripts/fetch_minute_candles.py
```
*   Скрипт скачивает свечи только для тех сигналов, у которых их еще нет.
*   **Важно:** Это может занять время (ограничение API Binance). Запускайте в `screen` или `tmux`, если сигналов много.

### 3. Отчет по качеству сигналов
Посмотреть, как отрабатывали сигналы за последнее время (без учета стратегии торговли).
```bash
python3 scripts/report_signals_30d.py --days 30
```
*   Покажет Win Rate (сколько сигналов дали >5%, >10%).
*   Покажет средний рост и время до пика.

---

## ⚙️ Оптимизация Стратегии (Раз в неделю/месяц)

Когда рынок меняется, старые настройки Stop-Loss и Trailing Stop могут стать неэффективными.

### 1. Запуск поиска параметров
```bash
python3 scripts/optimize_advanced.py
```
*   Скрипт переберет тысячи комбинаций SL, TS Activation и TS Callback.
*   Использует **реальные 1-минутные свечи** для максимальной точности.
*   Автоматически определит оптимальный **Timeout** (время удержания позиции).

### 2. Интерпретация результатов
Скрипт выведет "TOP 3 Configurations". Выберите ту, которая дает лучший баланс между `Avg Profit` и `Win Rate`.
Например:
```
Best: SL=-5%, Activation=15%, Callback=2%, Timeout=12h
```

---

## 📈 Бэктестинг Портфеля

Проверка того, как стратегия работала бы на реальном счете с учетом комиссий и рисков.

### Запуск реалистичного бэктеста
Используйте параметры, полученные на этапе оптимизации:
```bash
python3 scripts/backtest_portfolio_realistic.py --sl -5 --activation 15 --callback 2 --timeout 12
```

**Что учитывает этот скрипт:**
*   ✅ **Комиссии биржи** (0.05% за вход + 0.05% за выход).
*   ✅ **Проскальзывание (Slippage)** (0.1% — цена исполнения хуже идеальной).
*   ✅ **Ликвидации** (потеря всей маржи при просадке -8% с плечом 10x).
*   ✅ **Фандинг** (плата за удержание позиции).
*   ✅ **Умный выход по таймауту** (Hybrid Strategy: закрытие в плюс сразу, ожидание безубытка при малом минусе).

---

## 🛠 Обслуживание и FAQ

### Как часто обновлять данные?
*   **Сигналы (`populate`)**: Раз в день.
*   **Свечи (`fetch`)**: Сразу после обновления сигналов.

### Когда делать "Полный сброс"?
Если вы подозреваете, что данные в базе повреждены или хотите полностью пересчитать метрики (например, изменили логику расчета):
```bash
python3 scripts/populate_signal_analysis.py --days 60 --force-refresh
```
*   `--force-refresh`: Удалит все старые записи за этот период и загрузит заново.
*   ⚠️ После этого нужно будет заново скачать свечи для удаленных сигналов (если они удалились каскадно) или скрипт `fetch` просто проверит их наличие.

### Скрипт `pump_scanner.py`
Это "боевой" скрипт для реального времени. Он должен работать постоянно (как сервис systemd или в screen).
*   Мониторит базу на появление новых сигналов.
*   Шлет уведомления в Telegram.
*   Не требует ручного запуска для аналитики.

### Где лежат логи и настройки?
*   Настройки БД и Telegram: `config/settings.py` (или `.env`).
*   Логи: Выводятся в консоль. При запуске через systemd — в journalctl.

---

## 📋 Чек-лист перед торговлей

1.  [ ] Данные обновлены (`populate` + `fetch`).
2.  [ ] Оптимизация проведена на свежих данных (последние 30 дней).
3.  [ ] Бэктест с новыми параметрами показывает прибыль с учетом комиссий.
4.  [ ] Параметры бота обновлены в соответствии с результатами.
